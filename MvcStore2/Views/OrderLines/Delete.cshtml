@model MvcStore2.Models.ORDER_LINES

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ORDER_LINES</legend>

    <div class="display-label">CANTIDAD</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CANTIDAD)
    </div>

    <div class="display-label">PRECIO</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PRECIO)
    </div>

    <div class="display-label">ASSIGNED_AMOUNT</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ASSIGNED_AMOUNT)
    </div>

    <div class="display-label">ORDER</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ORDER.CP_ENVIO)
    </div>

    <div class="display-label">PRODUCT</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PRODUCT.NAME)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
