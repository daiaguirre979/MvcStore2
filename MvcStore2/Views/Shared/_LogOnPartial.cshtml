@if(Session["user"] != null) {
    <text>Bienvenido <strong>@Session["user_name"]</strong>!
    [ @Html.ActionLink("Cerrar Sesión", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Iniciar Sesión", "LogOn", "Account") ]
}